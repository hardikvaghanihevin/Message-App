<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MessageApp.Fullscreen" parent="Base.Theme.MessageApp">
        <item name="android:windowBackground">@color/background_secondary</item>
        <item name="android:windowIsTranslucent">true</item>

        <!-- Color change when system change theme-->
        <!-- <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>-->

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/app_bar_color</item>
        <item name="android:windowLightStatusBar">true</item><!--Use light(false) | dark(true) text/icons-->

        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@color/app_navbar_color</item>

        <!--<item name="android:windowAnimationStyle">@style/WindowAnimationFade</item>-->
    </style>

</resources>